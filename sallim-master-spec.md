# sallim (살림) — 마스터 스펙

> 최종 갱신: 2026-08-14
> **프로젝트/레포 이름: `sallim`** (확정) · 앱 표시명 `살림`
> - 이전 후보였던 `chorekit`은 범위가 집안일로 한정돼 폐기. `homekit`(애플 상표), `housekit`(호텔 하우스키핑 SaaS, housekit.app) 모두 충돌로 배제
> - 영문 서비스명이 필요해지면 추후 별도 결정
>
> **통합 출처**: `cleaning-automation-spec.md`(재구성본) + `feature-room-cleanliness.md` + `conversation-context-2026-07-21.md` + `집안일_관리_앱_디자인.zip`(Claude Design 핸드오프)
>
> 🔶 = 시안에 없는 기존 기능(편입 방식 결정 필요) · ⚠️ = 결정 필요
>
> **이 문서가 최종 기준.** 로컬 원본 대조는 더 이상 필요 없음 — 지금 정리된 내용으로 진행.

---

## 1. 프로젝트 정의

**한 가구(household)의 가구원들이 함께 쓰는 살림 관리 앱.** 집안일·일정·가계를 한 곳에서 공유한다.

기존에 별도 후보였던 3개 프로젝트(① 집안일 관리 ② 가계부 ③ 일정 관리)를 **하나로 통합**. 세 도메인 모두 같은 기반(가구, 가구원, 공유, 실시간 동기화)을 필요로 하므로 따로 만들면 같은 것을 세 번 만드는 셈이다.

> 쿡킷과 동일하게 "한 가구, 가구원이 공유하는 앱" 개념.

### 목적

1. **본인이 실제로 쓸 앱** (최우선)
2. **이직 포트폴리오** — 타깃 기업: 카카오뱅크, 토스/토스뱅크/토스페이먼츠, 핀테크
3. **학습 도구** — Java/Spring/Oracle 경험을 Kotlin/Spring Boot/DDD로 확장

> 수익화는 완성 후 실사용 경험을 바탕으로 판단하기로 보류.

### 통합이 포트폴리오에 유리한 이유

- **집안일 하나만으로는 DDD/CQRS/EDA를 쓸 명분이 약했다.** "이 규모에 왜 DDD를?"에 답하기 어려움. 도메인이 셋이 되면 바운디드 컨텍스트 분리가 자연스럽게 설명된다
- **가계부가 결정적** — 거래·정산·분담·잔액은 금융 도메인 모델링. 카뱅·토스 면접에서 청소 스케줄러보다 할 얘기가 훨씬 많다

### 최대 리스크: 완성하지 못함

목적 1순위가 "실제로 쓸 앱"인데 셋을 다 만들면 쓸 수 있게 되기까지 3배가 걸린다. 출산 예정으로 가용 시간은 줄어드는 방향.

**대응: 설계는 셋 다 지금, 구현은 순서대로.**
- 바운디드 컨텍스트 경계는 지금 다 잡는다 (나중에 끼워 넣으면 반드시 꼬임)
- 공유 기반(`Household`, `Member`, 권한, 동기화)은 처음에 한 번만 제대로 만든다
- 이후 집안일 → 캘린더 → 가계부 순으로 하나씩. 기반이 있으므로 뒤 도메인은 붙이는 비용이 싸다

---

## 2. 앱 구조

### 탭 3개

```
홈(평면도 + 요약 스트립)  /  캘린더  /  가계부
```

프로필·설정은 **우상단 프로필 아이콘** (쿡킷과 동일 패턴).

### 왜 메인화면을 따로 두지 않는가

`메인/캘린더/집안일/가계부` 4탭도 검토했으나 두 가지 문제가 있어 배제:
1. 메인이 다른 세 탭의 요약본이 되어 **중복**
2. **평면도가 3단계 안쪽으로 묻힘** — 평면도는 이 앱의 정체성이자 유일한 차별점

**대신 홈 = 평면도 그 자체.** 평면도는 이미 대시보드(집 상태를 한눈에)이므로, 상단에 얇은 요약 스트립(`오늘 일정 3건 · 이번 달 지출 82만원`)만 얹으면 메인화면 역할을 겸한다.

> "들어오자마자 캘린더가 보이면 좋겠다"는 요구는 **요약 스트립이 대신 충족**한다. 오늘 일정 건수는 스트립에서 바로 보이고, 자세히 볼 때만 캘린더 탭으로 이동.
>
> ⚠️ 별도 메인화면은 **필요해지면 나중에 추가**. 지금은 만들지 않음.

### 기존 시안(4탭)과의 매핑

| 시안 | 신규 구조 |
|---|---|
| 평면도 (기본) | **홈** — 그대로 유지 + 요약 스트립 추가 |
| 평면도 편집 모드 | 홈 하위 화면 — 그대로 유지 |
| 오늘·일정 | **캘린더 탭으로 흡수** (할 일이 일정에 함께 표시) |
| 할일 (리스트/상세) | 홈에서 진입하는 하위 화면 |
| 기록 (통계) | 홈에서 진입하는 하위 화면 |
| — | **가계부 탭 신설** |

시안 화면은 대부분 그대로 쓰이고 **진입 경로만 바뀐다.** 디자인이 버려지지 않음.

---

## 3. 도메인 구조 (바운디드 컨텍스트)

```
┌─────────────────────────────────────────┐
│  Household (공유 기반) — 가장 먼저 구현    │
│  Household · Member · 권한 · 동기화        │
└─────────────────────────────────────────┘
        ↑            ↑             ↑
   ┌────┴───┐   ┌────┴────┐   ┌───┴────┐
   │ Chore  │   │Calendar │   │ Ledger │
   │ 집안일  │   │  일정   │   │ 가계부  │
   └────────┘   └─────────┘   └────────┘
        1)          2)            3)
```

**설계 원칙**: 세 도메인은 `Household`를 공유하되 서로 직접 참조하지 않는다. 도메인 이벤트로만 소통 (예: 가계부의 "정기 결제일"이 캘린더에 일정으로 표시되는 것은 이벤트 구독으로).

---

## 4. 집안일 (Chore) — 상세 스펙

### 4.1 평면도 캔버스

- 100×100 **퍼센트 좌표계**. 방은 `left:x%; top:y%; width:w%; height:h%`
- 캔버스 높이 404px(Nocturne) / 398px(Organic)
- 편집 스텝 2%, `w ∈ [8, 100-x]`, `h ∈ [6, 100-y]`
- L자 공간은 사각형 2개를 z-index로 겹쳐 표현 (현관↑거실, 안방욕실↑안방)

**방 타일 레이어** (아래→위): 더러운 베이스 → 깨끗 오버레이 → 먼지 도트 → 얼룩 그라디언트 → 완료 애니메이션 → 라벨 행

**완료 애니메이션** (방의 미완료가 0이 되는 순간): 링 반짝임 900ms + 닦임 스윕 800ms, 상태값은 950ms 후 자동 해제

**편집 방식**: 템플릿 없이 자유 편집(방 이름·위치·크기·추가·삭제·되돌리기). 본인용 우선이므로 채택. 시안 권장대로 화살표 버튼 + 드래그/리사이즈 핸들 병행.
> 2/3/4베이 템플릿 선택은 **출시 검토 시 온보딩용으로 추가** (우선순위 하향)

### 4.2 ✅ 청결도 모델 — 확정

기존 스펙(시간 경과 decay)과 시안(미완료 개수)이 충돌했으나 **통합안으로 확정**.

```
시간 경과 → 주기 도래 → 할 일 인스턴스 자동 생성 → 미완료 누적 → 방이 더러워짐

먼지 = f( Σ 미완료 할 일마다 (1 + 지연일수 × 계수) )
```

- 주기가 돌아와야 더러워지기 시작 → 체크리스트 UX와 어긋나지 않음
- 그 뒤로는 밀린 날짜만큼 계속 짙어짐 → "오래 방치했다"가 표현됨
- 기존 `DecayPolicy` 전략 패턴은 `RecurrencePolicy`로 이름·역할 재정의하여 존속 ("다음 인스턴스를 언제 생성할지 결정")

**배제된 안**
- 순수 시간 기반: 할 일이 안 밀렸는데 방이 더러워 보여 "뭘 하라는 거지?"가 됨
- 순수 개수 기반: 8일 밀린 것과 14일 밀린 것이 동일. 할 일 미등록 방은 영원히 깨끗

### 4.3 도메인 모델

| 요소 | 타입 | 역할 |
|---|---|---|
| `Room` | 엔티티 | 방 이름, 타입 — **평면도 좌표는 여기 두지 않음** |
| `FloorPlan` | 값객체 | 퍼센트 좌표 배치(x/y/w/h/z), `RoomId` 참조만 |
| `ChoreDefinition` | 엔티티 | 할 일 정의: 라벨, 담당자, 주기, 방법 단계, 참고 영상 |
| `ChoreInstance` | 엔티티 | 특정 날짜에 생성된 인스턴스. 완료 여부·완료자·완료시각 |
| `RecurrencePolicy` | 정책/전략 | 주기별 다음 인스턴스 생성 시점 결정 |
| `CleanlinessScore` | 값객체 | 미완료 인스턴스 + 지연일수 기반 산출 |
| `ChoreCompletedEvent` | 도메인 이벤트 | 완료 시 발행 → 청결도 갱신 + 통계 반영 |

**설계 원칙**: `Room`은 이름·타입만 갖는 순수 도메인. 평면도 좌표는 `FloorPlan`으로 분리하여 표현 관심사가 도메인을 오염시키지 않게 한다.

### 4.4 시안 데이터 구조 (참고)

```
Room = { id, name, x, y, w, h, z, fs, tasks: Task[] }
Task = { label, who: '나'|'짝꿍', cycle: '매일'|'주 n회'|'주 1회'|'매달',
         how: string[], yt: string }
done = { [`${roomId}:${taskIndex}`]: bool }
```

**시드 데이터**: 시안 `DEFAULT_ROOMS`에 방 10개 + 할 일 18개(방법 3단계 + 유튜브 검색어) 완비 → 그대로 이식

---

## 5. 캘린더 (Calendar) — 개요

- 가구원 공유 캘린더
- **집안일 인스턴스가 일정으로 함께 표시** (시안의 `오늘·일정` 탭 흡수)
- 가계부의 정기 결제일도 이벤트 구독으로 표시
- ⚠️ 상세 스펙 미작성 — 집안일 구현 후 착수

---

## 6. 가계부 (Ledger) — 개요

- 가구 공동 가계부. 가구원별 지출 기록 + 정산
- 분담 통계는 집안일의 분담 통계와 동일한 "공정성 시각화" 컨셉으로 연결
- 정기 지출/구독은 반복 규칙을 집안일과 **공유 가능** (`RecurrencePolicy` 재사용 검토)
- ⚠️ 상세 스펙 미작성 — 캘린더 구현 후 착수

> 포트폴리오 관점의 핵심 소재: 거래 정합성, 정산 알고리즘, 이벤트 소싱 적용 여지

---

## 7. 타깃 기술 스택

**설계 기준**: 카카오뱅크·토스 채용 공고 공통 요구사항
- 토스뱅크: 서버 전체를 Kotlin으로 구성
- 카카오뱅크: Java 11+ 또는 Kotlin, Spring Boot + JPA. 우대 = Redis/Kafka/Spring Cloud, MSA 전환, 대용량 트래픽, WebFlux

| 영역 | 선택 | 근거 |
|---|---|---|
| 언어 | **Kotlin** (JDK 21) | 토스는 Kotlin 필수, 카뱅은 둘 다 허용 → 교집합을 덮음. "Java→Kotlin 전환" 서사가 학습능력 증명 |
| 프레임워크 | Spring Boot 3.x | 양사 공통 |
| 데이터 접근 | JPA(Hibernate) + QueryDSL | 양사 공고에 JPA 명시 |
| DB | MySQL | 핀테크 표준 |
| 캐시 | Redis | 양사 우대사항 |
| 메시징 | Kafka | 도메인 간 통신 + 양사 우대사항 |
| 아키텍처 | DDD, Hexagonal, CQRS, 모듈러 모놀리스 | — |
| 인프라 | Docker, Kubernetes | 카뱅에 쿠버네티스 직군 별도 존재 |
| CI/CD | GitHub Actions, ArgoCD | 토스 생태계에서 ArgoCD 사용 |
| 모니터링 | Prometheus, Grafana, Sentry, 분산추적 | — |
| 테스트 | JUnit5, Kotest, Testcontainers | — |
| 부하 테스트 | k6 또는 Gatling | 아래 참고 |
| 클라이언트 | ⚠️ 미정 — React Native / Flutter / Android Compose | 서버 포지션이면 최소 노력. Kotlin 통일 원하면 Compose |

**제외**: Python(타깃 무관), DynamoDB(관련도 낮음), PostgreSQL(MySQL로 통일)

### ⚠️ 주의점 2가지

**1. MSA로 처음부터 쪼개지 말 것**
도메인이 셋으로 늘었어도 아직 MSA 규모는 아니다. **모듈러 모놀리스로 시작하되 바운디드 컨텍스트 경계를 명확히** 하고, "쪼갤 수 있게 설계했지만 규모상 쪼개지 않았다"를 설명하는 쪽이 훨씬 성숙한 시그널이다.

**2. "대용량 트래픽 경험"을 어떻게 메울 것인가**
양사 공통 키워드인데 개인 앱은 트래픽이 없다. 보완책: **k6/Gatling 부하 테스트 → 병목 발견 → 개선 기록.** (N+1 제거 전후, Redis 캐시 도입 전후 TPS, Kafka로 동기 처리 분리 등) 숫자와 개선 과정이 남아야 이야깃거리가 된다. **나중에 붙이기 어려우므로 처음부터 염두에 둘 것.**

---

## 8. 구현 순서

**단계별 학습 Phase 구조는 폐기.** 처음부터 목표 스택으로 바로 구현한다. (Kotlin/Spring이 목표이고 이미 Java/Spring 경험이 있으므로 순수 클래스부터 쌓는 학습 단계가 불필요)

### 기반
1. 프로젝트 스캐폴딩 (Kotlin + Spring Boot 3.x + Gradle, 모듈러 모놀리스)
2. `Household` / `Member` / 권한 / 인증
3. 실시간 동기화 기반

### 집안일 (1순위 도메인)
4. 도메인 모델 + 단위 테스트
5. 영속성 (JPA + MySQL, Testcontainers 통합 테스트)
6. API 레이어 (Spring Boot REST)
7. 반복 인스턴스 생성 스케줄러 (자정 배치)
8. 도메인 이벤트 → Kafka → 통계 갱신 (EDA)
9. 통계 조회 분리 (CQRS)

### 확장
10. 캘린더 도메인
11. 가계부 도메인
12. 알림 채널 (이메일/카카오톡)
13. 모니터링 + 부하 테스트

> 학습을 위해 일부러 나눈 게 아니라 **의존 순서상 자연스러운 순서**다.

---

## 9. 미해결 갭

1. **반복 규칙이 단순함** ⚠️ — 시안 `cycle`은 `매일/주 n회/주 1회/매달`뿐. 경쟁 앱 최대 불만이 "매달 몇째 주 무슨 요일" 미지원인데 아직 반영 안 됨. **차별점이 될 지점.** 가계부의 정기 지출과도 공유되므로 우선순위 상승
2. **가전제품 유지보수 추적** 🔶 — 기존 스펙의 핵심 기능인데 시안에 없음. 주기가 월/년 단위라 방 청소와 성격이 다름. 별도 도메인으로 뺄지 집안일에 편입할지 결정 필요
3. **이메일 / 카카오톡 알림** 🔶 — 기존 스펙에 있으나 시안에 미반영. 채널 설계 필요
4. **가구원 확장성** — 시안은 2인 고정(`나`/`짝꿍`). 아이 포함 등 확장 시 구조 변경 필요
5. **할 일 편집 UI** — 시안은 읽기 전용 목업. 실제로는 추가/삭제/방 배정/담당자/주기/방법/영상 URL 편집 필요
6. **클라이언트 프레임워크 미정**
7. ~~테마 선택~~ — ✅ 확정: Nocturne + Organic 둘 다 지원
8. **캘린더·가계부 상세 스펙 미작성**

---

## 10. 디자인 시스템 (시안 확정값)

레이아웃·정보구조·인터랙션은 **양쪽 동일**, 토큰만 다름.
**✅ 확정: 처음부터 두 테마 모두 지원.** 토큰 맵 1개 + 테마 2개(Nocturne/Organic) 구조로 구현.

### Nocturne (다크)
```
bg #161826 · surface #232532 · text #e9e9ed · accent #9184d9
font: Inter 400/500/600 (headings weight 500 — 더 굵게 쓰지 말 것)
radius: sm 4 · md 8 · lg 14
icons: Phosphor
규칙: primary 액션은 accent 아웃라인(채우지 않음), accent는 선과 글로우로만, 순수 흑백 금지
```

### Organic (웜)
```
bg #f5ead8 · surface #ebddc5 · text #201e1d · accent #c67139 · sage #7a8a5e
font: Caprasimo(headings) / Figtree(body)
radius: sm 8 · md 16 · lg 28 · 버튼·칩·행은 999px pill
icons: Lucide, stroke-width 2.75
규칙: 날카로운 모서리 금지, 회색으로 탈색 금지, sage는 보조색이 아니라 두 번째 목소리
```

**공통**: 기기 프레임 390×844 · 상태바 46px
**톤**: 부부/파트너 2인 가구 대상, 살짝 유머러스한 한국어 반말·구어체
**접근성**: `:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px }`

> 시장 조사의 "게이미피케이션 톤 조절(성인 부부에겐 유치할 수 있음)" 우려를 잘 피한 편 — 먼지/반짝임 연출은 있으나 포인트·뱃지는 없음

---

## 11. 평면도 데이터 조사 결론

주소 기반 자동 평면도 로딩은 **제도적으로 불가능**하다고 판단. 좌표 데이터로 재구성하는 시안 방식 채택.

| 경로 | 결론 |
|---|---|
| 주소 → 자동 로딩 | ❌ 건축물현황도 발급은 다중이용건축물 한정, **주거용 층 제외** |
| `blcm.go.kr/api/DrawSearch.do` | ⚠️ API 실존하나 주거용 평면도 거의 없음, 배치도 위주 |
| `open.eais.go.kr` | ⚠️ 배치도 무료, 주거용 평면도 제한 |
| 세움터 본인 발급 | ⚠️ 소유자는 가능, 비소유자는 증빙서류 + 승인 대기 |
| 네이버 부동산 | ❌ 공식 API 없음, 크롤링 약관 리스크, 품질 편차 |
| 아키스케치 (12만 도면) | ⚠️ Enterprise API만, 개인 프로젝트 부적합 |

**핵심 판단**: 정확한 치수는 청소 필요도 판단에 무의미하고, "한눈에 들어옴"은 도면이 아니라 **색상 대비**가 만든다.

---

## 12. 시장 조사 요약

### 강점 벤치마킹
- **Tody**: 조건 기반 청소 관리, 더러움/청소 효과 시각화, FairShare(분담 불균형 시각화)
- **Home Tasker**: 실시간 일정 관리, 가사도우미/가족 분담
- **Homsy**: "기억하기"가 아니라 "공평하게 분배하고 눈에 보이게 만들기"에 집중
- **Chaos**: AI 기반 리마인더·일정·작업 통합, 최근 리뷰 1위

### 우리 프로젝트 기회
1. **동기화 안정성** — OurHome은 가족 간 동기화 버그로 사실상 서비스 중단(2023.09 구글플레이 비공개 전환). **도메인이 셋으로 늘면서 동기화 난이도가 올라가므로 더 중요해짐**
2. **경직된 반복 규칙** — "매달 몇째 주 무슨 요일" 미지원 불만 → 갭 #1
3. **공정한 분배의 시각화** — 집안일 분담 + 가계 분담 양쪽에 적용 가능
4. **게이미피케이션 톤 조절** — 성인 부부에겐 유치할 수 있음 → 시안이 잘 대응

---

## 13. 진행 방식 / 협업 원칙

- **Claude 사용 분리**: claude.ai(기획/설계/리서치) ↔ Claude Code(구현)
- **이력 관리**: 프로젝트 내 대화는 자동 참조되지 않으므로, 중요 결론은 `.md`로 정리해 Project Knowledge 업로드 → "짧은 대화 + 쌓이는 문서"
- **Agent Teams는 시기상조**: 역할 분리형 멀티 에이전트는 Pro 플랜 기준 토큰 소모 과다. 단일 세션 + 요약용 서브에이전트로 충분
- **벤치마킹 원칙**: 경쟁 앱 UI/디자인을 베끼지 않고 기능/흐름 수준에서만 참고
- **디자인 모호성 대응**: `CLAUDE.md`에 아키텍처 규칙·디자인 원칙 문서화 → 불필요한 질문 감소

---

## 14. 개발 환경 노트

- 개발 환경: IntelliJ IDEA 권장 (Kotlin/Spring 전환에 따라 VS Code에서 변경 검토) + Claude Code
- Claude Pro 구독 + Claude Code 세팅 완료
- Claude Code 세션은 폴더 경로 기준(`~/.claude/projects/`)으로 분리 저장. 다른 워크스페이스를 열면 이전 세션은 목록에서 안 보이지만 삭제된 것은 아님

---

## 15. 관점 노트

- AI를 단순 생산성 도구가 아니라 **비즈니스 프로세스 자체를 재설계하는 촉매**로 보는 시각 → 설계 방향에 반영

---

## 16. 다음 작업

### 착수 전 결정
- [x] 이름 확정 — `sallim`
- [x] 테마 — Nocturne + Organic 둘 다 처음부터 지원
- [ ] ⚠️ Kotlin 확정 (대안: Java 17+, 단 토스 어필 약화)
- [ ] ⚠️ 클라이언트 결정 (React Native / Flutter / Android Compose) — Claude Code에서 스캐폴딩하며 결정 권장

### 즉시 착수
- [ ] 프로젝트 스캐폴딩 (모듈러 모놀리스 구조, 3개 컨텍스트 경계 포함)
- [ ] `CLAUDE.md` 작성 — 아키텍처 규칙 + 디자인 원칙
- [ ] `Household` / `Member` 도메인 모델
- [ ] 집안일 도메인 모델 + 단위 테스트 (4.3장)
- [ ] 시드 데이터 이식 (시안 `DEFAULT_ROOMS`)

### 이후
- [ ] 갭 #1: 유연한 반복 규칙 (집안일 + 가계부 정기지출 공용)
- [ ] 갭 #2: 가전 유지보수 편입 방식 결정
- [ ] 갭 #3: 알림 채널 설계
- [ ] 캘린더 상세 스펙
- [ ] 가계부 상세 스펙
- [ ] 할 일 편집 UI 설계
- [ ] 부하 테스트 + 개선 기록 (포트폴리오 핵심 소재)
- [ ] 평면도 템플릿(2/3/4베이) — 출시 검토 시
