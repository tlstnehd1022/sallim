CREATE TABLE room (
    id CHAR(36) NOT NULL,
    name VARCHAR(255) NOT NULL,
    x INT NOT NULL,
    y INT NOT NULL,
    w INT NOT NULL,
    h INT NOT NULL,
    z INT NOT NULL,
    PRIMARY KEY (id)
);
